#include <iostream>
#include <vector>
using namespace std;

bool cmp(pair<int, string> p1, pair<int, string>, int s)
{
    if (p1.first > s)
        return
}

int main()
{
    int n, m, s;
    cin >> n >> m;
    vector<pair<string, int>> v(n);
    for (int i = 0; i < n; ++i)
    {
        cin >> v[i].first >> v[i].second;
    }
}